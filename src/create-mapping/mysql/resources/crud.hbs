var mysql = require("promise-mysql");
var con = mysql.createPool({
    host: "{{server}}",
    user: "{{user}}",
    password: "{{password}}",
    database: "{{database}}"
});

module.exports = {
    {{insert}},
    {{update}},
    {{selectAll}},
    {{searchByColumn}},
    {{deleteByColumn}}
}